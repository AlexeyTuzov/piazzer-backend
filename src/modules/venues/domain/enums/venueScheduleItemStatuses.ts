enum VenueScheduleItemStatuses {
    CREATED = 'created',
    DECLINED = 'declined',
    APPROVED = 'approved',
    CONFIRMED = 'confirmed',
    CANCELED = 'canceled'
}

export default VenueScheduleItemStatuses;